from graph_builder.context_diff import (
    generate_context_diff
)


def export_context_diff(
    old_context,
    new_context
):

    return generate_context_diff(
        old_context,
        new_context
    )